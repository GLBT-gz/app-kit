# 依赖版本统一策略（app-kit）

> 本文件是 **app-kit 仓库的依赖版本唯一权威**。AI 在修改任何 `Cargo.toml` 或 `package.json` 之前，必须先读本文件，遵守其中的规则。

## 1. 角色定位

app-kit 是**跨仓库共享的框架层**：`glbt-apps`（公司层）和 `my-apps`（个人层）都通过**精确相对路径**引用本仓库的共享库，**不经过 registry、不复制代码**：

- `shared/core` → `appkit-core`（通用能力库，feature 门控命令）
- `shared/automation` → `appkit-automation`（浏览器自动化）
- `shared/ui` → `@appkit/ui`（前端组件与 api.ts 封装）

因此本仓库是依赖版本事实上的**上游源头**：升级本仓库的依赖版本，会影响下游所有引用项目。改动前必须评估下游影响。

## 2. 单一版本来源

Rust 依赖版本一律写在根 `Cargo.toml` 的 `[workspace.dependencies]`；`template` 及共享库用 `xxx.workspace = true` 引用，**禁止写死版本号**。

规范版本表（= 根 `[workspace.dependencies]`）：

| crate | 版本 / features |
|---|---|
| serde | 1, features=[derive] |
| serde_json | 1 |
| anyhow | 1 |
| thiserror | 1 |
| tracing | 0.1 |
| tokio | 1, features=[full] |
| dirs | 6 |
| reqwest | 0.12, features=[json, blocking] |
| base64 | 0.22 |
| aes-gcm | 0.10 |
| rand | 0.8 |
| sha2 | 0.10 |
| opener | 0.7 |
| winreg | 0.52 |
| tokio-tungstenite | 0.24, features=[native-tls] |
| futures-util | 0.3 |

> `dirs` 必须是 6：下游 glbt-apps / my-apps 已统一到 6，若本仓库降到 5，下游会出现双版本。

## 3. 升级依赖的流程（影响下游，务必执行）

1. 修改 `[workspace.dependencies]`（或 package.json 版本）。
2. `cargo check` 本仓库全部成员。
3. 对**所有下游仓库**执行 `node ../scripts/check-all.mjs`（或至少对引用了被改 crate 的项目执行 `cargo check`）。
4. 若下游因 API 破坏报错，修复后一并提交；禁止用"各项目单独锁旧版本"的方式绕开（会制造双版本漂移）。

## 4. 下游引用约定（勿破坏）

下游仓库通过相对路径引用本仓库：

- `appkit-core` / `appkit-automation`：`../../../../app-kit/shared/...`（glbt-apps）、`../../../../app-kit/shared/core`（my-apps）
- `@appkit/ui`：`file:../../../app-kit/shared/ui`

**不要**：重命名目录、移动 `shared/*`、或要求下游改用 registry 版本。改动共享库路径 = 破坏所有下游项目。

命令体系按 feature 门控（`cmd-browser` / `cmd-files` / `cmd-utils` / `cmd-db` / `cmd-full` / `cmd-lite`），详见 `README.md`。

## 5. 已知不可统一的传递版本（不要试图修复）

以下双版本由 **tauri 生态传递引入**，不是本仓库能控制的，`cargo check` 后出现它们是正常的，**禁止**为此改任何 Cargo.toml：

| crate | 多出的版本 | 引入者 |
|---|---|---|
| reqwest 0.13.x | 与直接依赖的 0.12 并存 | tauri 2 自身 |
| winreg 0.55.x | 与 0.52 并存 | embed-resource（tauri-build 的构建依赖） |
| base64 0.21.x | 与 0.22 并存 | swift-rs（tauri 构建依赖） |

## 6. 前端依赖

- 根目录 `pnpm-workspace.yaml` 是唯一前端 workspace，单一 `pnpm-lock.yaml`；`shared/ui` 与 `template` 是成员，`@appkit/ui` 内部用 `workspace:*` 引用。
- 修改 `shared/ui` 依赖后运行根目录 `pnpm install` 更新锁文件；发布 `@appkit/ui` 的新版本（改 package.json version）后，下游用 `file:` 引用的项目会在各自 `pnpm install` 时自动拿到最新代码。

## 7. 校验命令

```powershell
cargo check
pnpm install --frozen-lockfile
pnpm -r typecheck
node scripts/check-commands.mjs   # 命令注册一致性（tauri_bridge.rs / build.rs / api.ts）
node ../scripts/check-all.mjs --frontend   # 组织级全量（含下游两个仓库）
```

提交前必须保证 `cargo check` 与 `pnpm install --frozen-lockfile` 通过。
